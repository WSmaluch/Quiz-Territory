# Environments

We use `staging` and `production` separate projects.
- Staging (`quiz-territory-staging`): Contains disposable sessions and generated demo packages.
- Production (`quiz-territory-production`): Protected public packages.
