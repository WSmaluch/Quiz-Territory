# Backup & Recovery

Prior to release execution, manually trigger a Firestore export task within Cloud Console. Expired sessions are removed automatically; completed attribution references are kept permanently per `docs/CLEANUP.md`.
